name: Check Format

on: [push, pull_request]

jobs:
  black check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable
        with:
          options: "--verbose"
